<nav class="navbar navbar-expand-sm navbar-dark bg-primary justify-content-center">
		  <ul class="navbar-nav mr-auto">
					<li class="nav-item">
						<a class="nav-link" href="/"><p font="Tekton Pro">Products</p><span class="sr-only">(current)</span></a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="/brands"><p font="Tekton Pro">Brands</p><span class="sr-only">(current)</span></a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="/categories"><p font="Tekton Pro">Categories</p><span class="sr-only">(current)
						</span></a>
					</li>
					<li class="nav-item active">
						<a class="nav-link" href="/customers"><p font="Tekton Pro">Customers</p><span class="sr-only">(current)
						</span></a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="/orders"><p font="Tekton Pro">Orders</p><span class="sr-only">(current)
						</span></a>
					</li>
		        </ul>	
		</nav>
		<br>
	<div class="container">
		<h1 class="mt-3 text-center">{{first_name}}  {{last_name}}</h1>
		<div class="col-lg-5 col-md-5 col-sm-8 mb-0 mx-auto">
		<div class="bg-info text-white p-3">
		<h4 class="card-title">ID: {{customer.id}}</h4>
		<h5 class="card-title">Email: {{email}}</h5>
		<h5 class="card-title">Address: {{street}}, {{municipality}}, {{province}}, {{zipcode}}</h5>
		</div>
		</div>
		</div>
		<div class="jumbotron jumbotron-fluid mt-2">
		<h5 align="center">Orders</h5>
		<div class="col-lg-12 col-md-12 col-sm-12 mb-2 mx-auto">
			<table style="width:100%" class="table table-sm">
				<tr>
					<th>Order ID</th>
					<th>Order Date</th>
					<th>Product ID</th>
					<th>Product Name</th>
					<th>Quantity</th>
				</tr>
				{{#each data}}
				<tr>
					<td>{{this.id}}</td>
					<td>{{this.order_date}}</td>
					<td>{{this.product_id}}</td>
					<td>{{this.product_name}}</td>
					<td>{{this.quantity}}</td>
				</tr>
				{{/each}}
			</table>
		</div>
	</div>